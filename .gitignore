node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
out/
storybook-static/
/test-results/
/playwright-report/
# Artefacts de test locaux (baselines visuelles, sorties de convergence) — gates locaux, pas sur git.
tests/
*.log
.DS_Store
.env*.local

# standalone_export : source de vérité (arbre extrait tracké).
# On exclut l'archive et les blobs redondants/lourds :
standalone_export/La carte du menu.zip
standalone_export/.thumbnail
standalone_export/uploads/
standalone_export/export/*.html

# Worktrees git créés par performance-guardian (fixes perf isolés)
.claude/worktrees/
