# standalone_export/

Zone de dépôt des **exports Claude Design** (zip ou décompressé). **Source de vérité** du design system et des pages — voir `CLAUDE.md` §1.

Contenu attendu : `_ds/<nom>/` (tokens, `_ds_manifest.json`, `readme.md` invariants, `_adherence.oxlintrc.json`), `planche/*.jsx` (source des pages), `assets/photos` + `uploads` (vraies photos ~2000px), `guidelines.md` + `Guide animations & QA.md`, previews `*.html`, `export/` (standalone + `og-image.jpg` 1200×630).
